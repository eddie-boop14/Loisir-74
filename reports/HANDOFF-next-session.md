# HANDOFF — Fable sweep close (Wave-3 + Stage-3 + hub prose + photos)

**Attested at close:** `main` = branch `claude/new-session-eua199` (see git log; tree clean
after the photo-ping log commit) · 427 fiches · **28 stations ×12 langs, all with verified
Wikimedia heroes** · 99 winter nodes, **43 carrying applied values** · winter-candidates
**48/53 confirmed** (5 honest nulls) · stations-de-ski hub: 28 cards + full prose (6 langs).
**Code re-attests before acting on anything below.**

## SHIPPED (this session, all live on loisirs74.fr)
1. **Stations Waves 1-3 (28 fiches ×12 langs)** + `stations-de-ski` hub (12 locales,
   prose in 6). Every wave: official sources, prices null, domain totals attributed,
   frozen names verbatim, dedupe honored (Le Semnoz/Salève/Sixt stayed as-is).
2. **JOB B winter apply + Stage-3 pass** — 49 payload rows applied via
   `scripts/apply_winter_facts.py` (⚑ Eddie sign-offs on all closed/partial régimes);
   **inforoute74 escape hatch** live (fiche card + facet md + facet JSON + gate rule 4b).
3. **Débaroulé correction** (La Clusaz, 12 langs): operator says playful trail, not
   rail-luge — reworded, logged.
4. **28 Wikimedia heroes** — licenses verified via the Commons API (single batched
   query; CC0/PD/CC BY/CC BY-SA only), credits + research_log per fiche, ≤1600px + webp
   + dims; Châtel/Morzine panoramas center-cropped. Protected-placements manifest
   refreshed alongside the one approved carrying-page change (EDMASTER-APPROVED trailer).
5. **IndexNow**: waves (120+120+108), JOB B freshness (396), photos+hub (348) — all
   HTTP 200, logged in `reports/indexnow-pings.json`.

## STANDING LAWS (unchanged)
Official sources only · nulls stay honest · frozen FR names ×12 · partner carrying pages
byte-frozen (EDMASTER-APPROVED trailer + manifest refresh is the only sanctioned path) ·
IndexNow hand-fired only · dedupe before authoring · **commit the FR checkpoint before
any long agent wait** (reclaim law).

## QUEUE (small, Eddie's call)
1. **google_check lane** — manual-only GitHub workflow (`sweep-loisirs74.yml`,
   cron-less since the July-1 dead-key incident). The 28 station fiches are staged
   (`google_check.query` set, `checked: null`); one manual dispatch on GitHub covers them.
   Do NOT re-cron; needs the Google key secret.
2. **Winter last-5 nulls** — plateau-de-loex, telecabine-pleney (luge), sentier-eterlou
   (luge), cascade-de-chedde (access), bords-de-l'Arve (access): nothing official found;
   they stay null until sources appear. Payload format reusable via `apply_winter_facts.py`.
3. **`station` ∉ WINTER_NODES** — extending the winter card to stations = separate
   decision (needs a ski_alpin vocab key).
4. **La Clusaz hero is portrait** (1200×1600) — fine under CSS cover, but a landscape
   Commons alternative could be swapped in at leisure.

## KEY TOOLING
`apply_winter_facts.py` (--report/--apply, ⚑ workflow) · `indexnow_ping.py` (hand-fired) ·
Commons verification: batch the API (50 titles/query — sequential calls get throttled) ·
`backfill_webp.py` + `build_img_dims.py` after any img drop (needs Pillow: `pip install
Pillow`) · 28-gate battery + `audit_photo_layer` = the green bar · scratchpad *.html
debris from research agents breaks canonical/meta gates — `rm scratchpad/*.html` first.

*North star: every line carries its source; the Département referees live road state;
nulls stay honest until verified. The station shelf is full and photographed.*

**© 2026 · Bleu canard édition · Edmaster & Claudius · Tous droits réservés 🦆**
