# link-integrity-allowlist — pre-existing broken internal targets, one per line.
# Empty: the 57 legal/footer i18n links are now real 301s in _redirects
# (legal-link cleanup), so nothing needs suppressing. Keep it empty.
